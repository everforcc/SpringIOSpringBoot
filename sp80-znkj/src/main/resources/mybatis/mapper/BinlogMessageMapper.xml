<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cc.sync.binlog.dao.BinlogMessageMapper">
    <insert id="insert">
        INSERT INTO binlog_message (
            database_name,
            table_name,
            sql_type,
            sql_list_json,
            binlog_file,
            binlog_pos,
            event_time,
            created_at
        ) VALUES (
                     #{database},
                     #{table},
                     #{sqlType},
                     #{sqlListJson},
                     #{binlogFile},
                     #{binlogPos},
                     #{timestamp},
                     NOW()
                 )
    </insert>

    <select id="selectByTimeRange" resultType="cn.cc.sync.binlog.dto.BinlogMessageEntity">
        SELECT *
        FROM binlog_message
        WHERE database_name = #{database}
          AND table_name = #{table}
          AND event_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY event_time ASC
    </select>
</mapper> 