<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cc.huifu.refund.mapper.ZnPayOrderRecordMapper">

    <cache
    eviction="FIFO"
    flushInterval="60000"
    size="512"
    readOnly="true"
    />


    <!-- 缓存导致@DB不生效 -->
    <select id="listZnPayOrderRecord" resultType="cn.cc.huifu.dto.ZnPayOrderRecord" useCache="false">
        SELECT json_unquote(JSON_EXTRACT(e.other_data,'$.hf_seq_id')) as otherDataHfSeqId,e.pay_amt,e.req_date,e.create_time,e.other_data,e.* FROM zn_pay_order_record e
        WHERE e.req_date = ${reqDate} and e.pay_amt is not null and (e.auth_type = '1' or e.auth_type is null)
        <if test="list != null and list.size() > 0">
            and json_unquote(JSON_EXTRACT(e.other_data,'$.hf_seq_id')) not in
            <foreach collection="list" item="hf" open="(" separator="," close=")">
                #{hf}
            </foreach>
        </if>
    </select>

    <select id="listZnPayOrderRecordLkl" resultType="cn.cc.huifu.dto.ZnPayOrderRecord" useCache="false">
        SELECT e.req_seqId as otherDataHfSeqId,e.pay_amt,e.* FROM zn_pay_order_record e
        WHERE e.req_date = ${reqDate} and e.pay_amt is not null and e.auth_type = '2' and e.acct_split_info is null
        <if test="list != null and list.size() > 0">
            and e.req_seqId not in
            <foreach collection="list" item="hf" open="(" separator="," close=")">
                #{hf}
            </foreach>
        </if>
    </select>

    <select id="listZnPayOrderRecordLklSplit" resultType="cn.cc.huifu.dto.ZnPayOrderRecord" useCache="false">
        SELECT e.req_seqId as otherDataHfSeqId,e.pay_amt,e.* FROM zn_pay_order_record e
        WHERE e.req_date = ${reqDate} and e.pay_amt is not null and e.auth_type = '2' and e.lkl_split_status = 'SUCCESS'
        <if test="list != null and list.size() > 0">
            and e.req_seqId not in
            <foreach collection="list" item="hf" open="(" separator="," close=")">
                #{hf}
            </foreach>
        </if>
    </select>

</mapper>