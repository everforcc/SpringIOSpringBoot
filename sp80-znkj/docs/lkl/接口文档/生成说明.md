### 新增商户进件-接口字段

- 根据图片生成对应的dto，正确的字段类型，是否必填，长度限制，详细描述，注释 命名V3TkbsMerchantEncryRequest 非空用@NotBlank，长度用@Size
- 本模块的包路径cn.cc.lkl.dto.merchantencry
- 要求处理所有的字段，用Lombok，接口说明，业务拓展信息，费率信息，附件信息都设置为单独的dto
- 要求可以将对应的json直接转成对应的dto，使用jackson
- 同时生成对应的ddl，放到当前文档的ddl目录下